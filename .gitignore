# Node.js
node_modules/
npm-debug.log
yarn-debug.log
yarn-error.log

# Environment Variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# MCP config: the project ships .mcp.json (vetted, no hardcoded secrets, ${VAR} only).
# Per-developer overrides go in .mcp.local.json, which stays out of git.
!.mcp.json
.mcp.local.json

# Build Output
dist/
build/

# IDE and Editors
.idea/
.vscode/
*.swp
*.swo
record/
*.mdc

# Operating Systems
.DS_Store
Thumbs.db 